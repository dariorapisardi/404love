{
  "name": "404love-sdk",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm --prefix packages/next run build",
    "test": "npm --prefix packages/next run test",
    "test:integration": "npm --prefix packages/next run test:integration",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
